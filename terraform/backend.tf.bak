# ------------------------------------------------------------------------------
# Terraform Backend Configuration
# ------------------------------------------------------------------------------
# 
# このファイルは、Terraform状態ファイルをS3に保存するための設定です。
# 
# 事前準備：
# 1. S3バケットの作成
# 2. DynamoDBテーブルの作成（ロック用）
# 
# 詳細な手順は COMPLETE_DEPLOYMENT_GUIDE.md を参照してください。
# ------------------------------------------------------------------------------

terraform {
  backend "s3" {
    # S3バケット名（事前に作成が必要）
    bucket = "mutual-app-terraform-state"
    
    # 状態ファイルのキー（パス）
    key    = "frontend/terraform.tfstate"
    
    # リージョン
    region = "ap-northeast-1"
    
    # 暗号化を有効化
    encrypt = true
    
    # ロック用のDynamoDBテーブル（事前に作成が必要）
    dynamodb_table = "terraform-state-lock"
    
    # AWSプロファイル（AWS_PROFILEで指定することも可能）
    # profile = "admin"
  }
} 